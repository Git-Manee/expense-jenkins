- name: Ansible Jobs Create
  hosts: localhost
  tasks:
    - name: Create Jenkins Jobs
      community.general.jenkins_job:
        config: "{{ lookup('file', 'job.xml') }}"
        name: test1
        password: admin123  #jenkins UI password
        url: http://172.31.91.152:8080 #jenkins server private IP
        user: Admin-MK #jenkins UI user name
